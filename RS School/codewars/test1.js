function capitalizeWord(word) {

word[0] = 'a';

// console.log (word[0].toUpperCase());
    word[0] = word[0].toUpperCase();
    console.log (word);
  //  return word[0].upper() + word[1:];
    // return `Hello, ${word[0].toUpperCase()}${word.substring(1,).toLowerCase()}!;
}

capitalizeWord("фвавыафыва");
//console.log (word[0]);





